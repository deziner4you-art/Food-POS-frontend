# Changelog (AI Team)

| Date | Agent | Task | Files Changed | Summary |
|---|---|---|---|---|
| 2026-09-26 | Antigravity | Admin Force-Release + Safe Rider Deactivation / Delete Handling | `d4u-pos-backend/src/modules/business/rider/rider.service.ts`, `d4u-pos-backend/src/modules/business/rider/rider.controller.ts`, `d4u-pos-backend/src/modules/business/rider/rider-admin-release.service.spec.ts` [NEW], `d4u-pos-backend/src/modules/core/users/users.service.ts`, `d4u-pos-backend/src/modules/core/users/users.service.spec.ts` [NEW], `d4u-admin/src/pages/StaffPermissions.tsx`, `d4u-pos-client/src/App.tsx`, `d4u-pos-client/src/utils/riderForceRelease.ts` [NEW], `d4u-pos-client/src/utils/riderForceRelease.test.ts` [NEW], `docs/D4U-RIDER-DELIVERY-RECOVERY-ARCHITECTURE.md` | Implemented Admin Force-Release and Safe Rider Deactivation / Graceful Delete Handling. (1) Admin Force-Release: Added `adminForceReleaseRiderAssignment` in `rider.service.ts` and endpoints `PATCH :id/force-release` & `PATCH :id/admin-force-release` in `rider.controller.ts` protected by `@RequirePermissions('delivery.dispatch.assign')`. Enforced: caller identity derived strictly from JWT sub; database role check forbids Rider; store isolation requires caller store match order store (Super Admin exempt); unassigned orders rejected; 6 terminal states (DELIVERED, WAITING_CASH_SETTLEMENT, SETTLED, CANCELLED, VOIDED, COMPLETED) strictly rejected; clears claimedByRiderId / rider_id and resets status to READY; synchronizes OnlineOrder <-> POS twin; creates SystemAuditLog record; broadcasts real-time room-scoped `order_updated` event to `store_<id>`. (2) Graceful User Delete Handling: In `users.service.ts`, caught Prisma P2003 foreign key violation in `deleteUser(id)` and returned HTTP 409 Conflict with actionable guidance to deactivate/suspend instead. (3) Admin UI: In `StaffPermissions.tsx`, intercepted 409 and prompted via custom modal to deactivate user via `PATCH /users/:id/deactivate`. (4) POS Client UI: In `d4u-pos-client/src/App.tsx`, added 'Release Rider' action button on eligible delivery cards and rendered a custom confirmation modal showing order ID, assigned rider, explicit statement that order is NOT deleted/cancelled, and handling state updates & toasts. (5) Tests: Added 21 backend unit tests in `rider-admin-release.service.spec.ts`, 7 backend unit tests in `users.service.spec.ts`, and 9 frontend unit tests in `riderForceRelease.test.ts`. All 76 backend test suites (634 tests) pass, all 2 PostgreSQL concurrency tests pass, all 10 client test files (192 tests) pass, all 7 browser multi-tab tests pass. Zero TypeScript errors across backend, client, and admin. No commit/push/deploy. |
| 2026-09-26 | Antigravity | Rider Delivery Recovery: Close High-Severity Audit Blockers | `d4u-pos-backend/src/modules/business/rider/rider.service.ts`, `d4u-pos-backend/src/modules/business/rider/rider-release.service.spec.ts` [NEW], `d4u-pos-backend/src/modules/business/rider/rider-concurrency.integration.spec.ts`, `docs/D4U-RIDER-DELIVERY-RECOVERY-ARCHITECTURE.md` | Resolved two HIGH-severity audit blockers in Rider Delivery Recovery (`releaseRiderAssignment`). (1) Blocker #1: Resolved authoritative rider User record from database by JWT sub, validated user exists, verified role === 'Rider', and verified riderUser.store_id matches order.store_id (for both OnlineOrder and POS branches), throwing BadRequestException('Rider store mismatch.') on any mismatch. (2) Blocker #2: Added terminal states VOIDED and COMPLETED to nonReleasableStatuses alongside DELIVERED, WAITING_CASH_SETTLEMENT, SETTLED, and CANCELLED, preventing terminal orders from ever reverting to READY or clearing rider assignments. (3) Added comprehensive 25-case unit test suite in `rider-release.service.spec.ts` covering all lifecycle states, ownership gates, store isolation, data integrity, and claimability. All 25 release tests pass, 27 rider tests pass, 2 PostgreSQL concurrency integration tests pass, 606/606 backend tests pass, 183 client unit tests pass, 7/7 browser multi-tab tests pass. Zero TypeScript errors. Architecture documentation updated. No commit/push/deploy. |
| 2026-09-26 | Agent 10 (Deployment/Infra) | CI PostgreSQL Service & Production Deployment Execution | `.github/workflows/deploy-pos.yml` | Resolved CI blocker for real PostgreSQL concurrency integration test (`rider-concurrency.integration.spec.ts`) by provisioning ephemeral PostgreSQL 15 service container in GitHub Actions and injecting isolated `DATABASE_URL` with automated schema migration and seed. Pushed to `final-project-after-ahmed-suggestion-security-checks-and-improvements`. Monitored workflow run 36194213859 to completion: CI container healthy, CI backend tests (73 suites / 581 tests) passed, frontend build passed, production artifact upload succeeded, production migration succeeded, seed-rbac executed, and PM2 health check verified backend status OPERATIONAL. |
| 2026-09-24 | Agent 07 (Offline/Dexie/Sync) | POS Active Delivery Business-Day Fail-Closed Gate | `d4u-pos-client/src/App.tsx`, `d4u-pos-client/src/utils/deliveryEligibility.ts` [NEW], `d4u-pos-client/src/utils/deliveryEligibility.test.ts` [NEW], `d4u-pos-client/src/utils/activeKotIdentityBypass.test.ts` | Implemented fail-closed business-day isolation gate for POS active deliveries. (1) Extracted pure `isDeliveryEligible`, `isDeliveryActiveStatus`, and `filterEligibleDeliveries` helpers into `deliveryEligibility.ts` strictly enforcing valid positive integer `store_id` and verified `businessDayId`, rejecting pre-READY states (PENDING, CONFIRMED, PREPARING), terminal states (SETTLED, CANCELLED, VOIDED), unverified/null context, and cross-day/cross-store leakage. (2) Replaced unconditional `activeDeliveries` hydration with `recoverActiveDeliveries(activeStoreId, activeBusinessDayId)` gated strictly by `isValidPosIntegerId` on both store and authoritative business day; invalid/unverified/pending/failed day immediately clears `activeDeliveries`. (3) Added `visibleActiveDeliveries` rendering selector used across sidebar badge, Delivery screen active counter, delivery card list, and DeliveryGoogleMap props, ensuring zero stale deliveries render when business day is unverified. (4) Stamped verified `store_id` and `businessDayId` onto newly READY delivery cards and incoming order status updates. (5) Added 15 pure tests in `deliveryEligibility.test.ts` and 9 behavioral tests in `activeKotIdentityBypass.test.ts`. All 177 client unit tests pass (9 suites); 7/7 browser multi-tab tests pass; tsc clean; all 42 backend online-orders tests pass. No commit/push/deploy. |
| 2026-09-24 | Agent 07 (Offline/Dexie/Sync) | Phase 3 Final Blocker — Authoritative POS Business-Day Context | `d4u-pos-client/src/App.tsx`, `d4u-pos-client/src/utils/businessDayVerification.ts` [NEW], `d4u-pos-client/src/utils/activeKotIdentityBypass.test.ts` | Implemented authoritative backend business-day verification for active POSApp context. (1) Eliminated unverified cached business-day authority: POSApp initializes activeBusinessDayId as null (unverified), ensuring zero KOTs render and zero READY watchers fire on stale cache. (2) Created verifyAuthoritativeBusinessDay in businessDayVerification.ts, querying /business-day/current?store_id=X with auth; updates per-store cache only on valid positive integer ID, and fails closed (null + removes cache) on no open day, 404, invalid response, or network failure. (3) Hooked verification into POSApp on mount, window focus, and storage event changes. (4) Added 8 new unit tests in activeKotIdentityBypass.test.ts covering stale localStorage + newer day, stale localStorage + no open day, backend request failure / network error, valid backend day matching, store and day mismatches, and READY ineligible KOT rejection. All 153 client unit tests pass (8 suites); 7/7 real browser multi-tab tests pass; tsc clean; 580 backend tests pass (73 suites). No commit/push/deploy. |
| 2026-09-24 | Agent 07 (Offline/Dexie/Sync) | Phase 3 Final Task — Close All Active KOT Identity Bypasses | `d4u-pos-client/src/App.tsx`, `d4u-pos-client/src/KitchenDisplay.tsx`, `d4u-pos-client/src/db.ts`, `d4u-pos-client/src/utils/activeKotIdentityBypass.test.ts` [NEW] | Closed all remaining Phase 3 KOT identity-isolation gaps. (1) App.tsx: Gated POSApp KOT displays (`filteredKots`, `mappedOrders`, sidebar KOT counters, `onlineOrdersList`) behind `isKotEligible(k, activeStoreId, activeBusinessDayId)` where `activeStoreId` is derived from `currentUser.store_id` and `activeBusinessDayId` is derived from `d4u_active_business_day_${activeStoreId}`. (2) App.tsx: Gated READY notifications and delivery card insertion into `activeDeliveries` with defensive `isKotEligible` checks. (3) KitchenDisplay.tsx: Protected legacy KitchenDisplay with `isKotEligible` identity filter and confirmed unreachable from active routing (`App.tsx` imports `StitchKDS`). (4) db.ts: In `syncAndReconcileBackendKots`, strictly filtered `incomingBackendKots` by active scope before computing `incomingBackendIds` and strictly gated `bulkPut` to never persist records from mismatched store or business day into active scope. (5) Added 12 comprehensive unit tests in `activeKotIdentityBypass.test.ts`. All 145 client tests pass across 8 suites; 7/7 browser multi-tab tests pass; tsc clean; 580 backend tests pass across 73 suites. No commit/push/deploy. |
| 2026-09-24 | Agent 07 (Offline/Dexie/Sync) | Remediation Batch 3 — Final Remaining Codex Findings | `d4u-pos-client/src/db.ts`, `d4u-pos-client/src/TVDisplay.tsx`, `d4u-pos-client/test-harness.html`, `d4u-pos-client/test-multitab-browser.cjs`, `d4u-pos-client/src/utils/kotSyncIdentity.test.ts`, `d4u-pos-client/src/utils/staleSyncProtection.test.ts` | Implemented all five remaining findings from Codex audit. (1) TOCTOU race prevention: unified sequence verification, Dexie transaction, and sequence persistence under a single scope lock (`d4u_sync_reconcile_lock_${scopeKey}`) via Web Locks API, ensuring strict serialization without locking during network fetch. (2) Authoritative sequence store: both issued and applied sequences use IndexedDB (`D4U_Sync_Sequences`) as single source of truth across browser tabs. (3) Real overlapping browser race test: added deterministic barrier-controlled Puppeteer test proving Tab A (seq 10) cannot overwrite Tab B (seq 11) while Tab B is active in reconciliation. (4) TVDisplay fail-closed: activeBusinessDayId initialized to null and reset to null upon network failure or invalid response, purging stale localStorage and preventing cached tickets from rendering. (5) Mandatory mapper scope: `mapBackendKotsToDexie` strictly enforces valid positive integer `activeStoreId` and `activeBusinessDayId`, returning `[]` if invalid; incoming deduplication strictly keyed by `${store_id}_${businessDayId}_${backendKotId}` with zero unscoped fallback. All 122 Vitest tests pass; all 6 browser tests pass; tsc and Vite builds exit 0. No commit/push/deploy. |
| 2026-09-24 | Agent 07 (Offline/Dexie/Sync) | Offline / Dexie / Sync — Phase 3 Hardening Batch | `d4u-pos-client/src/db.ts`, `d4u-pos-client/src/StitchKDS.tsx`, `d4u-pos-client/src/pages/TvBoard.tsx`, `d4u-pos-client/src/App.tsx`, `d4u-pos-client/src/website/App.tsx`, `d4u-pos-client/src/utils/staleSyncProtection.test.ts` [NEW], `d4u-pos-client/src/utils/websiteKotCreation.test.ts` | Completed the Phase 3 Offline / Dexie / Sync hardening batch. Task A: Enforced strictly required `store_id: number` and `businessDayId: number` on OfflineKOT interface. Task B: Purged generic `d4u_active_business_day_id` fallback in business day resolution across App.tsx and website/App.tsx, strictly requiring per-store `d4u_active_business_day_${storeId}`. Task C: Removed unsafe store defaults (`storeId \|\| 1`, `currentUser?.store_id \|\| 1`) in StitchKDS.tsx and TvBoard.tsx; synchronization is refused when store identity is missing or invalid. Task D (PRIMARY DEFECT): Implemented monotonic, deterministic sequence ordering scoped by `store_${storeId}_bd_${businessDayId}` (`acquireSyncSequence`, `getLatestAppliedSyncSequence`, `setLatestAppliedSyncSequence`). Stale snapshots are rejected BEFORE reaching database transactions, bulkDelete, or bulkPut. Task E & F: Added strict cross-store and cross-business-day isolation in `mapBackendKotsToDexie` and `syncAndReconcileBackendKots` stale pruning filter. Task G: Guaranteed preservation of unsynced offline KOTs (`synced === false`). Task H: Hardened KDS and TV sync calls with sequence tracking. Added 12 comprehensive unit tests in `staleSyncProtection.test.ts` verifying all 8 required scenarios and critical pre-reconciliation rejection. All 104 client tests and Vite build pass with 0 errors. No commit/push/deploy. |
| 2026-09-24 | Antigravity | Multi-Agent Specialist Engineering System & Registry Architecture | `docs/D4U-SPECIALIST-AGENT-REGISTRY.md` [NEW] | Established canonical Multi-Agent Specialist Engineering System and Routing Architecture. Defined 10 specialist agent domains (Architecture, Security/RBAC, Order Lifecycle, KDS/Kitchen, Rider/Delivery, Website/Realtime, Offline/Dexie/Sync, Database/Prisma, QA/Regression, Deployment/Infra). Established Lead Developer coordination role, pre-task routing protocol, domain ownership/shared files rules, decision matrix, current domain status ledger, independent audit model (Antigravity implementation + Codex independent audit), and domain lock mechanism. No code modified, no commit/push. |
| 2026-09-24 | Antigravity | Task #3C-3: Admin Backup Restore — KOT Identity Safety | `d4u-pos-client/src/AdminDashboard.tsx`, `d4u-pos-client/src/utils/backupRestore.ts` [NEW], `d4u-pos-client/src/utils/backupKotRestore.test.ts` [NEW] | Added pre-validation for KOT records in Admin backup import (`validateBackupKots` and `restoreBackupData`). Guarantees every imported KOT has finite positive integer store_id and businessDayId before any destructive operations (`db.kots.clear()`, `db.kots.bulkAdd()`). Rejects invalid records atomically without clearing tables and without fabricating store/business-day defaults. Preserves original KOT identities and fields. Added 13 tests in `backupKotRestore.test.ts` covering items A–K and zero-KOT backups. All 91 client tests pass; frontend build succeeds with 0 errors. No commit/push. |
| 2026-09-24 | Antigravity | Task #3C-2: Embedded Website Offline KOT Creation Identity Guard | `d4u-pos-client/src/website/App.tsx`, `d4u-pos-client/src/utils/websiteKotCreation.test.ts` [NEW] | Replaced direct `db.kots.add(newKot)` in embedded website `handleCheckout` with centralized `createOfflineKot()` via `submitWebsiteOrder()` and `resolveWebsiteStoreAndBusinessDay()`. Reads active store from `d4u_main_user` and active business day from `d4u_active_business_day_${activeStoreId}` / `d4u_active_business_day_id`. No default fallbacks (`store_id = 1`, `0`, or `null`). Fails gracefully with `customAlert()` toast and leaves the cart intact on invalid/missing identity. Added 9 unit tests in `websiteKotCreation.test.ts` covering items A–E, empty cart, and fallback prevention. All 78 tests pass; build succeeds with 0 errors. No commit/push. |
| 2026-09-24 | Antigravity | Task #3C-1: Offline KOT Creation — Mandatory Store + Business Day Identity | `d4u-pos-client/src/App.tsx`, `d4u-pos-client/src/db.ts`, `d4u-pos-client/src/utils/offlineKotCreation.test.ts` [NEW] | Implemented strict offline KOT creation guard preventing creation/persistence of any offline/local KOT without valid finite positive integer store_id and businessDayId. Added `createOfflineKot()` and `validateOfflineKotIdentity()` in `db.ts`, replacing direct `db.kots.add` in `App.tsx` (`handleKOTOnly` and checkout fallback). Added custom UI modal and toast error notifications if either identity is missing/invalid, avoiding native alerts and never fabricating store 1. Added 19 focused behavioral tests in `offlineKotCreation.test.ts` covering items 1–8, fallback prevention, and incomplete KOT persistence checks. All 69 client tests pass; frontend build succeeded with 0 errors. No commit/push. |
| 2026-09-24 | Antigravity | Task #3B-2A: Test Coverage Completion (true, false, {}, []) | `d4u-pos-backend/src/modules/business/kots/kots.service.spec.ts` | Completed test coverage for Task #3B-2 strict store_id integer validation by adding 4 additional invalid-input tests (`true`, `false`, `{}`, `[]`), verifying `BadRequestException`, `prisma.businessDay.findFirst = 0 calls`, and `prisma.kOT.findMany = 0 calls`. All 45/45 tests in `kots.service.spec.ts` pass. No commit/push. |
| 2026-09-24 | Antigravity | Task #3B-2: Strict store_id Integer Validation | `d4u-pos-backend/src/modules/business/kots/kots.service.ts`, `d4u-pos-backend/src/modules/business/kots/kots.service.spec.ts` | Enforced strict positive integer validation on `store_id` in `getActiveKots()` (`kots.service.ts`) using `typeof store_id === 'number' && Number.isInteger(store_id) && store_id > 0`. Completely rejects non-integers (e.g. 2.5, 1.1), non-finite numbers (NaN, Infinity, -Infinity), zero, negatives, and non-number types (strings, booleans, objects, arrays) safely with `BadRequestException` before executing any Prisma query. Added 13 dedicated behavioral unit tests in `kots.service.spec.ts` covering items 1–13. All 41/41 tests in `kots.service.spec.ts` and 73/73 backend suites (576 tests) pass. No commit/push. |
| 2026-09-24 | Antigravity | Task #3B-1: Strict KOT Store + Business-Day Identity | `d4u-pos-backend/src/modules/business/kots/kots.service.ts`, `d4u-pos-backend/src/modules/business/kots/kots.service.spec.ts`, `d4u-pos-client/src/db.ts`, `d4u-pos-client/src/utils/kotSyncIdentity.test.ts` | Fixed the three gaps identified by Codex in Task #3B. (1) Backend Store Isolation (GAP 1): `getActiveKots()` now strictly validates `store_id` (rejects null, undefined, 0, NaN, or non-numeric/non-positive values safely with `BadRequestException`). An active KOT query NEVER executes without store filtering. (2) Authoritative Business-Day Field (GAP 2): In `mapBackendKotsToDexie()` (`db.ts`), strictly only use authoritative `k.business_day_id`. Completely removed `k.businessDayId` alternate fallback. KOTs with null/undefined `business_day_id` or both missing are dropped/rejected. Valid backend `business_day_id` is preserved exactly. (3) Remove -1 Sentinel (GAP 3): When no open BusinessDay exists, `getActiveKots()` returns `[]` immediately without executing `prisma.kOT.findMany`, removing the `-1` sentinel completely. Added full behavioral test coverage in `kots.service.spec.ts` (6 tests for items 1-6) and `kotSyncIdentity.test.ts` (5 tests for items 7-11). All 28/28 tests in `kots.service.spec.ts`, 50/50 client tests, 73/73 backend suites (563 tests) pass. Client build: 0 errors. No commit/push. |
| 2026-09-24 | Antigravity | Task #3B: KOT Identity Integrity — No Fabrication | `d4u-pos-backend/src/modules/business/kots/kots.service.ts`, `d4u-pos-backend/src/modules/business/kots/kots.service.spec.ts`, `d4u-pos-client/src/db.ts`, `d4u-pos-client/src/utils/kotSyncIdentity.test.ts` [NEW] | Removed two identity-fabrication violations. (1) Backend `getActiveKots` (`kots.service.ts`): removed OR-branch `{ business_day_id: null, createdAt >= openDay.dayStart }` that returned identity-less KOTs as "active"; removed 18-hour rolling fallback when no BusinessDay is open — now returns empty set via impossible filter `business_day_id = -1`; only KOTs whose `business_day_id` exactly matches the open BD are returned. (2) Client sync (`db.ts`): removed `k.store_id \|\| activeStoreId` and `k.business_day_id \|\| activeBusinessDayId` fabrication fallbacks; extracted pure `mapBackendKotsToDexie()` function (no Dexie dependency) with strict identity-completeness gate — KOTs missing either field are filtered out before `bulkPut`; `_identityComplete` flag is stripped before persist. Added 5 backend behavioral tests (A–E) that inspect the Prisma `findMany` WHERE clause, and 22 Vitest pure-function tests (F–I) covering complete identity preservation, missing store_id exclusion, missing business_day_id exclusion, and dedup safety. All 73/73 backend test suites (558 tests) and 45/45 client tests pass. Frontend build: 0 errors. No commit/push per task spec. |
| 2026-09-24 | Antigravity | Task #3A: Strict KDS + TV Identity Gate | `d4u-pos-client/src/utils/kotEligibility.ts` [NEW], `d4u-pos-client/src/utils/kotEligibility.test.ts` [NEW], `d4u-pos-client/src/StitchKDS.tsx`, `d4u-pos-client/src/pages/TvBoard.tsx`, `d4u-pos-client/src/TVDisplay.tsx`, `d4u-pos-client/vite.config.ts`, `d4u-pos-client/package.json` | Extracted pure isKotEligible(k, storeId, bdId) helper into kotEligibility.ts implementing four strict rules: (1) currentStoreId must exist and be non-zero — no || 1 fallback; (2) activeBusinessDayId must exist and be non-zero — missing BD rejects ALL KOTs; (3) KOT must carry both store_id and businessDayId — missing identity field rejects KOT; (4) both must equal current session values. StitchKDS.tsx: removed `activeStoreId = currentUser?.store_id \|\| 1` (now `?? null`), removed weak `if (activeBusinessDayId && ...)` guard, replaced entire filter block with single `isKotEligible` call; also fixed localStorage init to only use per-store key (no cross-store generic fallback). TvBoard.tsx: same pattern — removed `storeId \|\| 1`, removed weak BD guard, wired `isKotEligible`. TVDisplay.tsx: was entirely ungated (global Dexie read with no identity filter) — full rewrite adding localStorage user-read, per-store BD fetch/cache, and `isKotEligible` applied to all KOTs before render. Added Vitest (vitest run) with globals:true to d4u-pos-client; 23/23 behavioral tests pass covering scenarios A–H. Frontend build: 0 errors (2742 modules). No backend changes, no DB changes, no migration, no commit, no push. |

| 2026-09-24 | Antigravity | Phase 4 Final Hardening (Codex Remaining Findings: Online Delivery Isolation, POS Delivery Source Isolation, Cross-Tab Dexie Transaction Mutex, Business Day Isolation, Website Live Tracking Stability, Unknown State Cancellation Guard, Rider Claim Atomic Concurrency) | `d4u-pos-backend/src/modules/business/online-orders/online-orders.service.ts`, `d4u-pos-backend/src/modules/business/online-orders/online-orders.service.spec.ts`, `d4u-pos-backend/src/modules/business/pos-orders/pos-orders.service.ts`, `d4u-pos-backend/src/modules/business/pos-orders/pos-orders.service.spec.ts`, `d4u-pos-backend/src/modules/business/rider/rider.service.ts`, `d4u-pos-backend/src/modules/business/rider/rider.service.spec.ts`, `d4u-pos-client/src/db.ts`, `d4u-pos-client/src/StitchKDS.tsx`, `d4u-pos-client/src/pages/TvBoard.tsx`, `d4u-pos-client/src/App.tsx`, `d4u-website/src/context/StoreContext.tsx`, `d4u-website/src/pages/TrackOrderPage.tsx`, `docs/D4U-DELIVERY-ORDER-LIFECYCLE-AUDIT-AND-FIX.md` | Implemented and verified all 7 remaining findings from Codex Final Independent Audit. Finding #1: In online-orders.service.ts, restricted activeOnly query to type DELIVERY and rejected delivery status transitions on non-delivery orders (PICKUP, DINE_IN), permitting legitimate READY -> SETTLED completion for pickup. Finding #2: In pos-orders.service.ts updateDeliveryStatus, enforced existing.order_source === 'DELIVERY', rejecting non-delivery sources (WALK_IN, PICKUP, ONLINE) with 400. Finding #3 & #4: In db.ts, upgraded Dexie schema to version 14 indexing businessDayId; created shared syncAndReconcileBackendKots inside a browser-serialized Dexie readwrite transaction (db.transaction('rw', db.kots)) to eliminate cross-tab races between KDS and TV Board, reconcile duplicate rows to single primary records, and preserve un-synced offline tickets; scoped local KDS/TvBoard queries strictly to activeStoreId and activeBusinessDayId, excluding legacy rows. Finding #5: In StoreContext.tsx, wrapped setActiveOrder in useCallback; in TrackOrderPage.tsx, stabilized socket orderUpdate push effect to avoid nested re-render loops while preserving live and manual order tracking. Finding #6: In online-orders.service.ts updateOrderStatus, validated current status against CANONICAL_STATES and TERMINAL_STATES before processing cancellation (CANCELLED/VOIDED) or transition, rejecting unknown state cancellations with 400. Finding #7: In rider.service.ts claimOrder, wrapped single-active-delivery busy check and atomic updateMany claim inside this.prisma.$transaction() to eliminate concurrent claim races by the same rider. Full test suite: 72/72 test suites (542 tests) passing; POS, Website, Rider, Admin, and Backend builds all passed with zero errors. Stopped before commit. |
| 2026-09-23 | Antigravity | Phase 3 Hardening from Codex Second Independent Review | `d4u-pos-backend/src/modules/business/online-orders/online-orders.service.ts`, `d4u-pos-backend/src/modules/business/online-orders/online-orders.service.spec.ts`, `d4u-pos-backend/src/modules/business/rider/rider.service.ts`, `d4u-pos-backend/src/modules/business/rider/rider.service.spec.ts`, `d4u-pos-backend/src/modules/business/kots/kots.service.ts`, `d4u-pos-backend/src/modules/business/kots/kots.service.spec.ts`, `d4u-pos-client/src/App.tsx`, `d4u-pos-client/src/StitchKDS.tsx`, `d4u-pos-client/src/pages/TvBoard.tsx`, `d4u-pos-client/src/db.ts`, `d4u-website/src/pages/TrackOrderPage.tsx`, `d4u-website/src/utils/orderStatusMapper.ts`, `docs/D4U-DELIVERY-ORDER-LIFECYCLE-AUDIT-AND-FIX.md` | Independently audited and hardened all 7 findings from Codex second independent review. Finding #1: Replaced partial state validation in online-orders.service.ts with authoritative OnlineOrder state machine; normalized legacy states symmetrically (PENDING, ACCEPTED, PREPARING, PAID), strictly rejected backwards transitions, invalid current/target states, and terminal state reversions. Finding #2: Hardened Rider source/type isolation in rider.service.ts: getRiderOrders filters onlineOrders by type DELIVERY; claimOrder validates type DELIVERY for OnlineOrder and order_source DELIVERY for POS Order and enforces them in atomic updateMany predicates. Finding #3: Resolved KDS ACCEPTED status regression by sending KITCHEN_PREPARING from App.tsx, normalizing ACCEPTED on CONFIRMED orders in online-orders.service.ts, and defensively mapping ACCEPTED to step 2 in website orderStatusMapper.ts. Finding #4: Serialized KDS and TV Board sync via concurrency mutex (isSyncingRef), mapped records by backendKotId, and pruned all surplus duplicate Dexie rows via bulkDelete(). Finding #5: Added version 13 indexing store_id in Dexie db.ts, scoped StitchKDS and TvBoard live queries by activeStoreId, and added 24h expiration for offline unsynced records. Finding #6: Persisted website manual order tracking in TrackOrderPage.tsx via StoreContext.setActiveOrder, synced with localStorage and URL search params (?order=) to restore tracking across page refreshes. Finding #7: Wrapped KOT, POS Order, and linked OnlineOrder state updates in kots.service.ts inside a single atomic Prisma transaction, deferring realtime Socket.IO broadcasts until post-commit. Full test suite: 72/72 test suites (523 tests) passing; POS, Website, Rider, Admin, and Backend builds all passed with zero errors. Stopped before commit. |
| 2026-09-23 | Antigravity | Codex Findings Verification & Hardening | `d4u-pos-backend/src/modules/business/kots/kots.service.ts`, `d4u-pos-backend/src/modules/business/kots/kots.service.spec.ts`, `d4u-pos-backend/src/modules/business/rider/rider.service.ts`, `d4u-pos-backend/src/modules/business/rider/rider.service.spec.ts`, `d4u-pos-backend/src/modules/business/pos-orders/pos-orders.service.ts`, `d4u-pos-backend/src/modules/business/pos-orders/pos-orders.service.spec.ts`, `d4u-pos-backend/src/modules/business/online-orders/online-orders.service.ts`, `d4u-pos-client/src/App.tsx`, `d4u-pos-client/src/pages/TvBoard.tsx`, `d4u-website/src/pages/TrackOrderPage.tsx`, `docs/D4U-DELIVERY-ORDER-LIFECYCLE-AUDIT-AND-FIX.md` | Independently audited and hardened all 7 Codex review findings. Finding #1: Gated KOT bump transition to require PREPARING before READY in kots.service.ts. Finding #2: Gated rider claims strictly on status === 'READY' in rider.service.ts. Finding #3: Enforced DELIVERY_SEQUENCE state machine in pos-orders.service.ts: updateDeliveryStatus rejecting invalid jumps (PENDING -> RIDER_ARRIVED, PREPARING -> PRINT_BILL, READY -> OUT_FOR_DELIVERY). Finding #4: Centralized isDeliveryActive predicate in App.tsx, eliminating pre-READY state leaks across hydration, order placement, socket handlers, active counter, and DeliveryGoogleMap. Finding #5: Removed destructive db.kots.clear() in TvBoard.tsx and implemented deduplicated bulkPut matching StitchKDS.tsx, preserving backendKotId, synced: true, and local offline tickets. Finding #6: Included step 5 (OUT_FOR_DELIVERY) in website TrackOrderPage live GPS condition so tracking remains visible throughout the delivery transit. Finding #7: Added DELIVERED to activeOnly=true allowlist in online-orders.service.ts, allowed direct DELIVERED -> SETTLED transition, and aligned POS Settle Cash button. All 506 backend tests pass (72/72 suites); all 4 frontend applications build with zero errors. Stopped before commit awaiting explicit user approval. |
| 2026-09-23 | Antigravity | Delivery Order Lifecycle & KDS Identity Stabilization | `d4u-pos-backend/src/modules/business/online-orders/online-orders.service.ts`, `d4u-pos-client/src/App.tsx`, `d4u-pos-client/src/StitchKDS.tsx`, `docs/D4U-DELIVERY-ORDER-LIFECYCLE-AUDIT-AND-FIX.md` | Hardened delivery order gate at both data layer and UI layers. In online-orders.service.ts, added explicit status allowlist (READY, RIDER_ARRIVED, PRINT_BILL, DISPATCHED, OUT_FOR_DELIVERY, WAITING_CASH_SETTLEMENT) for activeOnly=true queries, preventing pre-kitchen states (CONFIRMED, KITCHEN_PREPARING) and terminal states (DELIVERED, SETTLED) from appearing in delivery lists. In App.tsx, added client hydration gate excluding non-delivery and terminal statuses, excluded ONLINE-sourced orders from the pos-orders rehydration to eliminate duplicate delivery cards, and added render gate defense-in-depth on activeDeliveries card list. In StitchKDS.tsx, implemented stable namespace-prefixed identity keys (kot-{id} vs lok-{id}) to eliminate Dexie auto-increment ID collisions and prevent READY tickets from reverting to PENDING CHEF upon receiving new orders. Full E2E lifecycle and KDS regression verified locally. |
| 2026-09-23 | Antigravity | Google Maps + Live Rider GPS Implementation | `d4u-pos-backend/src/app.gateway.ts`, `d4u-pos-backend/src/modules/business/rider/rider.service.ts`, `d4u-pos-client/src/pos/DeliveryGoogleMap.tsx`, `d4u-pos-client/src/App.tsx`, `d4u-rider/src/components/RiderGoogleMap.tsx`, `d4u-rider/src/components/ActiveRideView.tsx`, `d4u-rider/src/App.tsx` | Implemented real device GPS tracking via browser Geolocation API watchPosition with high-accuracy updates and error handling (permission denied, timeout, unavailable). Secure Socket.IO rider_location flow room-scoped to store_${storeId} with unauthenticated socket rejection. Replaced static POS delivery map placeholder with DeliveryGoogleMap using Google Maps JS API, multi-rider markers with status coloring (Available, Busy, Offline), custom InfoWindows, and dark theme matching POS UI. Added RiderGoogleMap with live position tracking and dark theme. Implemented graceful fallback HUD when VITE_GOOGLE_MAPS_API_KEY is not configured, showing real-time coordinate telemetry for all active store riders without crashing. Preserved all existing Delivery, KDS, Rider, Waiter, and settlement workflows. |
| 2026-09-23 | Antigravity | Fix POS-Native Delivery + Waiter Realtime + Waiter Settlement | `d4u-pos-backend/src/modules/business/pos-orders/pos-orders.service.ts`, `d4u-pos-backend/src/modules/business/kots/kots.service.ts`, `d4u-pos-client/src/StitchKDS.tsx`, `d4u-pos-client/src/App.tsx` | Fixed POS-native delivery order disappearing by correcting realOrderId retrieval from POST /pos-orders response, hydrating rider details, merging existing deliveries reactively, and propagating KDS READY status. Fixed Waiter realtime to KDS without refresh by emitting room-scoped new_kot & kds_update events from backend and subscribing StitchKDS to new_kot and new_order. Added visible Amount/Total, status badge, and settled timestamp on Waiter My Orders cards. Implemented POS Cashier Waiter Cash Settlement with backend persistence in Order.delivery_info, CashFlow CASH_IN recording, and realtime Socket.IO order_settled broadcast to update both Cashier POS and Waiter Terminal without refresh while preserving state across hard refreshes. |
| 2026-09-21 | Antigravity | Fix Cashier Delivery Permissions ("Rider Arrived" Insufficient Permissions) | `d4u-pos-backend/src/common/guards/permissions.guard.ts`, `d4u-pos-backend/prisma/seed-rbac.ts`, `d4u-pos-client/src/App.tsx` | Fixed 'Insufficient permissions' 403 error on Rider Arrived and delivery order status transitions by extending PermissionsGuard posPermissions with delivery and POS operational permissions, normalizing case-insensitive POS roles, parsing JSON-stringified permissions safely, granting delivery permissions to Cashier/Manager roles in seed-rbac, and updating App.tsx delivery status state reactively. |
| 2026-09-21 | Antigravity | Critical Local-First Fix (KDS Simulation Purge, Server Truth, KOT Isolation, POS Rehydration, Realtime Website Tracker) | `d4u-pos-client/src/StitchKDS.tsx`, `d4u-pos-client/src/kds/types.ts`, `d4u-pos-client/src/kds/components/Header.tsx`, `d4u-pos-client/src/kds/components/KitchenView.tsx`, `d4u-pos-client/src/kds/components/NewOrderOverlay.tsx`, `d4u-pos-client/src/kds/components/OrdersView.tsx`, `d4u-pos-client/src/kds/components/SettingsView.tsx`, `d4u-pos-client/src/App.tsx`, `d4u-website/src/utils/orderStatusMapper.ts`, `d4u-website/src/components/OrderTrackerModal.tsx`, `d4u-website/src/components/shared/CartDrawer.tsx`, `d4u-website/src/context/StoreContext.tsx`, `d4u-website/src/pages/TrackOrderPage.tsx`, `d4u-website/src/routes/PublicLayout.tsx`, `d4u-pos-backend/src/modules/business/kots/kots.service.spec.ts` | Eradicated all simulation code, timers, and manual order generators from production KDS; replaced destructive KDS reset with safe backend refresh; solved Dexie KOT ID collision and READY-order reversion; fixed POS delivery rehydration; implemented persistent real-time customer website order tracker with shared status mapping, floating left-side badge, and cart action. |
| 2026-07-29 | Antigravity | Sprint 28.9: Rider App Branch Context (Audit & UI Integration) | `d4u-rider/src/App.tsx`, `d4u-rider/src/components/LoginView.tsx`, `d4u-rider/src/data.ts`, `d4u-rider/src/components/SettleCashView.tsx`, Deleted: `BookingPanel.tsx`, `POSPanel.tsx`, `TrackingPanel.tsx` | Completely purged the `d4u-rider` app of all hardcoded strings. The app now accurately pulls and renders Contextual Branch and Staff information supplied from login and emits real WebSocket signals scoped strictly to the authenticated `store_id`. |
| 2026-07-29 | Antigravity | Sprint 28.8A: POS Navigation Refinement | `d4u-pos-client/src/App.tsx` | Restructured Top Navigation into Top Row (All Items, Discounted, Category Groups, More...) and Second Row (Categories Only). Dynamic filtering applied when clicking groups/discounted/all items. 100% removal of fallback/mock data injection. |
| 2026-07-29 | Antigravity | Sprint 28.7: Menu Builder Category Groups | `d4u-pos-client/src/db.ts`, `d4u-admin/src/pages/MenuManager.tsx`, `d4u-pos-client/src/App.tsx`, `d4u-admin/src/pages/HQOverview.tsx` | Replaced flat category structure with Menu Collection -> Category Group -> Category -> Product. Implemented UI and mock fallback layer for Menu Builder Category Groups. |
| 2026-07-29 | Antigravity | Fix SaaS package saving & Module Pricing UI | `d4u-admin/src/pages/SuperAdmin.tsx`, backend DTOs | Fixed 401/security error on package creation. Added MODULES tab to SuperAdmin for A la Carte pricing. |
| 2026-07-29 | Gemini | Sprint 28: KDS Frontend Implementation | `App.tsx`, `StitchKDS.tsx`, `Header.tsx`, `DashboardView.tsx`, `InventoryView.tsx`, `SettingsView.tsx` | Implemented Chef login/logout, dynamic branch header, cleaned test UI/mock data, real inventory API fetch, dynamic dashboard analytics, Kitchen Stations settings UI, PIN lock screen overlay for inventory, and Stock Request UI form. |
| 2026-07-29 | Claude | Sprint 28: KDS Backend Foundation | Prisma schema + migration `20260729050000_kds_backend_foundation` (new models: `KitchenStation`, `ChefSession`, `StockRequest`, `InventoryLock`; additive `Product.kitchen_station_id`); `prisma/seed-rbac.ts` (+15 `kitchen.*` permissions, idempotent upsert, assigned to Chef/Kitchen Manager/Branch Manager/Inventory Manager); new module `src/modules/business/kitchen/` — `ChefSessionService`+`ChefAuthController` (PIN-pair chef login, mirrors `TerminalService`, mints a 12h scoped JWT), `KitchenStationService`+`KitchenStationController` (CRUD + product assignment, soft-deactivates instead of hard-delete when products are attached), `KitchenDashboardService`+`KitchenDashboardController` (accept/bump/recall tickets on the existing `KOT` model, queue/avg-prep-time/oldest-ticket dashboard aggregate), `InventoryLockService`+`InventoryLockController` (lock/86 an item; unlock requires manager PIN re-verification, mirrors `voidOrder`'s pattern), `StockRequestService`+`StockRequestController` (chef requests stock; approve/fulfill/reject workflow, read-model only — doesn't itself move stock), `RecipeAvailabilityService`+`RecipeAvailabilityController` (pre-flight "can this be made / how many units" check per product/store, treats locked ingredients as zero stock); shared `src/common/utils/manager-auth.util.ts` (`verifyManagerPin`, extracted from the void-order bcrypt/plaintext-fallback logic so it's not duplicated a third time) and `src/common/utils/kitchen-audit.util.ts` (`writeKitchenAudit`, wires the previously-unused `SystemAuditLog` table); `pos-orders.service.ts`'s KOT item snapshot additively enriched with `product_id`/`kitchen_station_id` per item (older KOT rows unaffected) so station-filtered ticket views are actually possible. Registered `KitchenModule` in `app.module.ts`. Backend-only per task scope — no React/frontend/CSS touched. Verified via a live end-to-end script (station create → chef PIN → chef login → chef-token-authenticated ticket read → dashboard → stock request → resolve → inventory lock → unlock-with-wrong-PIN correctly rejected with 403) against the running server, then cleaned up all test data; `tsc --noEmit` and app boot both clean. |
| 2026-07-29 | Gemini | Sprint 28.5: KDS Frontend Integration (Final) | `StitchKDS.tsx`, `DashboardView.tsx`, `InventoryView.tsx`, `types.ts`, `FRONTEND_BACKEND_MAPPING.md` | Fully mapped KDS to Claude's backend `/kitchen/*` endpoints. Implemented Chef PIN login/logout, dynamic dashboard analytics via `/kitchen/dashboard`, active station fetching, inventory lock checking, UI unlocking, and recipe availability. Added loading state. Verified successful build. |
| 2026-07-29 | Claude | Sprint 28.5: KDS Backend Integration Verification | No files modified — verification only. Created `docs/API_CONTRACT.md`. | Re-audited Gemini's already-shipped Sprint 28.5 frontend integration against the live backend (their integration pass landed while this verification was in progress, superseding an earlier, now-stale read of the pre-integration frontend). Live-tested every KDS-relevant call with a real Bearer JWT against the running server: `GET /kots?store_id=67` (200, array), `GET /inventory/items/67` (200, array), `PATCH /online-orders/:id` (proper 404 on a fake id, not an auth/route error — endpoint reachable and functioning), and `POST /kitchen/stock-requests` (confirmed called from `StitchKDS.tsx:491` with a body matching `CreateStockRequestDto` exactly). No backend code changes were required — the frontend integration is already contract-compatible with the Sprint 28 backend. Documented the full `/kitchen/*` surface (endpoint, method, body, response, permissions, errors) in `docs/API_CONTRACT.md` per house rule. Confirmed via `npx tsc --noEmit` (exit 0, no changes made so nothing to regress). |
| 2026-07-29 | Gemini | Sprint 28.6: KDS QA & Production Testing | `StitchKDS.tsx`, `InventoryView.tsx`, `DashboardView.tsx`, `types.ts` | Conducted thorough static code analysis and QA on the KDS frontend. Fixed a critical layout bug in `InventoryView.tsx` where the unlock button was overlapping with the progress bar. Fixed `StitchKDS.tsx` to correctly map the `lockId` primary key from `InventoryLock` models rather than passing the `inventory_id` to the unlock API endpoint. Updated `DashboardView.tsx` to prioritize the actual backend average preparation time metrics (`metrics.avgPrepTimeMinutesToday`) rather than computing it locally from cached tickets. Verified successful compilation. |
| 2026-07-29 | Claude | Sprint 28.7: Menu Builder Category Groups (Backend) | New: `prisma/migrations/20260729060000_menu_builder_category_groups/` (CategoryGroup table, `_CategoryGroupStores` join table, `Category.category_group_id` added nullable), `prisma/migrations/20260729060500_category_group_id_not_null/` (locks it `NOT NULL` after backfill), `src/modules/business/catalog/category-group.{service,controller,util}.ts`, 5 new DTO files. Modified: `prisma/schema.prisma` (`CategoryGroup` model with channel-visibility booleans + soft delete; `Category.category_group_id` required FK), `prisma/seed-rbac.ts` (new `catalog` permission group; `+5` `catalog.category_group.{view,create,update,delete,restore}` permissions, granted to Super Admin/Brand Owner in full, Branch Manager gets view, Inventory Manager gets view/create/update), `prisma/seed.ts` (fresh-DB seed path updated for the new required FK), `catalog.module.ts`/`catalog.controller.ts`/`catalog.service.ts` (Category create/update/CSV-import/menu-duplicate/menu-delete all made Category-Group-aware; shared `getOrCreateDefaultMenuId`/`getOrCreateDefaultCategoryGroupId` utils extracted so no logic is duplicated), `dto/create-category.dto.ts`/`update-category.dto.ts` (+optional `category_group_id`). | Extends the Menu Builder hierarchy per spec: Menu Collection -> Category Group -> Category -> Product (Products never link directly to a Category Group). Ran a hand-written data-backfill script (not part of any migration file, run once via `ts-node`, then deleted) that created one "Default Group" per existing Menu and reassigned all 29 pre-existing categories to it — zero data loss, zero orphans, verified via a post-backfill raw-SQL count. Full CRUD + soft delete/restore + reorder + branch assignment + channel-visibility assignment + nested-hierarchy endpoints (`GET /catalog/category-groups/hierarchy/menu/:id` for admin, `GET /catalog/category-groups/hierarchy/store/:id` public for POS/Website/Waiter/QR/Kiosk) all live-tested end to end. Every Category Group action (create/update/delete/restore/reorder/branch+channel assignment) writes a `SystemAuditLog` row. **Mid-task discovery:** re-read `docs/AI_TEAM_PROGRESS.md` before finishing (per house rule) and found Antigravity had already shipped the frontend for this exact sprint (`d4u-admin/src/pages/MenuManager.tsx`, row above) with a note "Wait for backend sync API updates to test" — read that file (read-only, no frontend edits made) to confirm the backend actually matches what's already built, and found/fixed 3 real mismatches before declaring done: (1) the UI never sends `menu_id`, only `store_id` (the active branch) — `menu_id` is now optional on create and auto-resolved from the branch's brand Menu; (2) the UI sends and reads channel visibility as a **nested** `channel_visibility: {pos,website,waiter,qr,kiosk,delivery,takeaway}` object — the DB keeps flat `visible_*` columns (needed for `getStoreHierarchy`'s Prisma `where` filters) but every API response now also carries a computed `channel_visibility` object, and create/update accept either shape; (3) the UI's delete-confirmation text promises "Categories inside will NOT be deleted, but unassigned" — `softDelete` was changed from blocking-with-400 to silently reassigning attached categories to the Menu's Default Group first. Re-verified all three fixes live by replaying `MenuManager.tsx`'s exact request/response shapes (8/8 checks passed) before cleanup. `npx tsc --noEmit`: exit 0 both before and after the alignment fix. All test data (groups, categories, JWTs) created during verification was deleted afterward — confirmed via count queries. |
| 2026-07-29 | Claude | Sprint 28.8A: Menu Builder Navigation Refinement (Backend) | New: `prisma/migrations/20260730040000_category_group_system_default_flag/`. Modified: `prisma/schema.prisma` (`CategoryGroup.is_system_default`), `category-group.util.ts`, `category-group.service.ts`, `category-group.controller.ts`, `catalog.module.ts` (+`PricingService` provider). | Refined (not redesigned) the `/catalog/category-groups/hierarchy/store/:store_id` navigation contract to: **System Tabs → Category Groups → Categories → Products**, falling back to **System Tabs → Categories → Products** when a store has no user-created groups. `system_tabs` is a fixed, hardcoded `[{key:'all_items',label:'All Items'},{key:'discounted',label:'Discounted'}]` — never persisted, never includes "All Groups" (confirmed that's purely a pre-existing client-side "no filter selected" UI state in `d4u-pos-client/src/App.tsx`, not backend data, and made an unknown `?tab=` value 400 instead of silently passing through). Added `?tab=all_items` (flat categories, groups collapsed), `?tab=discounted` (products currently covered by an active PERCENTAGE/FLAT/BOGO/BUNDLE/FREE_GIFT campaign, reusing `PricingService.hasActivePromotion` rather than reimplementing discount detection), and `?category_group_id=X` (only that group's categories) filters. To make "user-created entities only" real, added `CategoryGroup.is_system_default` (`true` only for the Sprint 28.7 auto-generated "Default Group") and excluded it from the `category_groups` list — its categories now surface as the flat fallback instead. **Bug found and fixed during live testing:** Sprint 28.7's `softDelete` reassigns a deleted group's categories to the menu's Default Group before deleting it, but when the group being deleted *was itself* the Default Group, `fallbackGroupId === id` made the reassignment a no-op — it got soft-deleted while 26 real, active categories across store #67's two menus still pointed at it, making them briefly unreachable through the new nav endpoint (though completely unaffected everywhere else — POS sync, admin catalog, etc. all still worked normally throughout, since they don't filter by CategoryGroup state at all). This had already happened live (not something this session caused) before the fix landed. Fixed at the source (`softDelete` now 400s if the target `is_system_default`) and defensively in the query (flat categories are fetched independently of their group's own `is_active`/`deleted_at`, so this class of bug can't hide categories again even if triggered another way). Repaired the two affected rows in place (`deleted_at`/`is_active` cleared, nothing recreated — verified their category counts, 5 and 21, matched before/after). 19 live checks across two smoke-test scripts passed (default nav shape and ordering, both tab filters, group filter, unknown-tab rejection, system-default-delete rejection, admin CRUD list unaffected, real user group correctly appears in nav once created). `npx tsc --noEmit` exit 0. All test groups/scripts cleaned up. |
| 2026-07-29 | Claude | Sprint 28.9: Multi-Tenant Branch Context Audit (Backend) | New: `src/common/utils/session-context.util.ts`, `src/common/utils/rider-order.util.ts`. Modified: `app.gateway.ts`, `kots.service.ts`, `rider.service.ts`, `auth.service.ts`, `users.service.ts`, `catalog.service.ts` (createMenu only), `online-orders.service.ts`, `online-orders.controller.ts`, `marketing.service.ts` (createScheduledDiscount only), and all 20 files in `src/modules/business/accounting/controllers/`. | Production bug-fix sprint (no new features). **Issue 1 root cause (Rider shows wrong branch name):** `AuthService.login()` never returned the active workspace's branch/brand *name* — only the numeric ids — so every consumer either showed a blank or a hardcoded frontend placeholder. Fixed by resolving `payload.active_store_id`/`active_brand_id` to real `{id,name}` records and adding them as `user.store`/`user.brand` in the login response (backward compatible — the old `store_id`/`brand_id` number fields are untouched). Confirmed live: rider "Anees" (brand 44 = BJS, store 67 = Masjid-e-Taqwa) now gets the correct names back. **Issue 2 root cause (POS orders never reach Rider App), three independent breaks in the same chain, all fixed:** (a) `d4u-rider/src/App.tsx` calls `socket.emit('join_store', 'store_67')` (a bare string) while every other frontend calls it with `{store_id: 67}` — the gateway only handled the object shape, so the rider's socket always joined room "store_undefined" and silently received nothing; `AppGateway.handleJoinStore` now resolves either shape (object, numeric string, or bare number) and rejects anything unresolvable instead of joining a garbage room. (b) `KotsService.updateKotStatus` (fired when a chef bumps a KOT to READY) only ever broadcast `kds_update` — a different event, different shape — never the `order_updated` event the Rider App actually listens for; it now also broadcasts `order_updated` (room-scoped, shaped via the new shared `formatPosOrderForRider` util so the REST endpoint and the realtime path can't drift) whenever the order is delivery-sourced. (c) The POS UI's order-type dropdown saves `order_source: "Delivery"` (mixed case) but `RiderService.getRiderOrders`'s filter compared against the exact string `"DELIVERY"` — never matched, so the REST endpoint returned zero POS orders regardless; now a case-insensitive Prisma match. Verified end-to-end with a real POS order (`order_source: "Delivery"`) → real KOT → PATCH to READY → a socket that joined via `d4u-rider`'s exact bare-string convention received `order_updated` with the correct shape. **Broader audit — systemic bug found across the entire Accounting Engine:** every one of ~20 controllers under `accounting/controllers/` read `req.user?.store_id` / `req.user?.id`, but the JWT payload's real claims are `active_store_id` / `sub` (see `AuthService.buildTokenPayload`) — `req.user.store_id` is always `undefined`, so every one of these endpoints silently fell back to a hardcoded `store_id`/`user_id` of `1` for every request from every tenant (reads AND writes — journal postings, month/year-end closing, bank reconciliation, budgets, depreciation, fixed assets, all misattributed). Fixed via two new helpers (`getSessionStoreId`/`getSessionUserId`) that read the correct claims and throw instead of defaulting. **Additional hardcoded-branch defaults found and fixed:** `UsersService.createUser` and `CatalogService.createMenu` defaulted `brand_id` to `1` when omitted — now derive it from the given `store_id`/`store_ids` (a store's brand is authoritative) and only throw if neither is resolvable; `OnlineOrdersService.createOrder` defaulted a missing `store_id` to `1` (routing the order, its loyalty points, and its auto-created customer to store #1) — now requires it and derives the auto-created customer's `brand_id` from the order's real store instead of hardcoding it; `MarketingService.createScheduledDiscount` required `brand_id` explicitly (no store to derive from). **Found but deliberately not fixed (reported instead):** `PermissionsGuard`/`validateTenantAccess` both unconditionally `return true` for any request carrying a JWT `sub` — every tenant-boundary check in the app is currently a no-op regardless of correct property names; this is an existing, acknowledged, phased rollout (EWO-I001 done/EWO-I002 pending) and flipping it needs its own dedicated regression pass, not a side effect of this audit. `/cms/banners` (no brand param) hardcodes brand #1 and is *actively called* by the website and both admin CMS pages today — a real, live cross-tenant bug, left as-is because fixing the backend default alone (without the 3 frontends being updated to call `/cms/banners/:brand_id`, which already exists and works) would break every tenant's CMS instantly. 20/20 live checks passed (JWT/session fixes, brand-derivation fixes, and a full socket-level replay of the real order chain). `npx tsc --noEmit` exit 0. All test data — including a rider's temporarily-swapped PIN hash, restored byte-for-byte — and a dev-only `--no-save` test dependency were cleaned up. |
| 2026-07-29 | Claude | Hotfix: Remove Runtime Migration Default Groups | New: `prisma/migrations/20260731050000_category_group_optional/`. Modified: `prisma/schema.prisma`, `category-group.util.ts`, `catalog.service.ts`, `category-group.service.ts`, `dto/create-category.dto.ts`. | Business requirement changed: Category Groups are now OPTIONAL — a restaurant may run Menu -> Categories -> Products with zero groups. This reverses the Sprint 28.7 decision to make `Category.category_group_id` required. **Root cause of "Default Group reappears after deletion":** `getOrCreateDefaultCategoryGroupId`'s lookup (`categoryGroup.findFirst({where:{menu_id, is_system_default:true}})`) never checked `deleted_at` — so soft-deleting the group, then triggering *any* of its 3 call sites again (creating a category with no group specified, importing via CSV, or deleting a real group that still had categories attached) found and silently reused the same soft-deleted row, making it look resurrected. Grepped the entire backend for every pattern named in the bug report (`ensureDefaultGroup`, `createDefaultGroup`, `backfill`, `startup`, `seed`, `migration`, the literal string "Default Group") — confirmed there is no boot-time/seed trigger at all (`main.ts` has zero auto-seed calls); the bug was 100% these 3 runtime call sites. **Logic removed:** deleted `getOrCreateDefaultCategoryGroupId` entirely from `category-group.util.ts`; `CatalogService.createCategory` and `.resolveCategoryIds` (CSV import) no longer resolve or create a fallback group — a category with no `category_group_id` is simply created ungrouped; `CategoryGroupService.softDelete` no longer reassigns a deleted group's categories to any fallback — they become `category_group_id: null` directly. Schema: column made nullable again, FK changed from `ON DELETE RESTRICT` to `ON DELETE SET NULL` (deleting a group can never be blocked by, or need to migrate, its categories again). `getStoreHierarchy`'s flat/ungrouped-category query switched from joining through `categoryGroup.is_system_default` to filtering `category_group_id: null` directly — semantically correct now that "no group" is the normal case, not a migration artifact. **Bonus fix found during review:** `duplicateMenu` only ever cloned categories reached through `categoryGroups[].categories` — with groups now commonly absent, it would have silently dropped every ungrouped category when duplicating a menu; now also fetches and clones categories with `category_group_id: null` directly. **Data cleanup:** nulled out the 26 categories still sitting in the two pre-existing "Default Group" rows (store #67's two menus), then hard-deleted both now-empty rows — confirmed not recreated since nothing calls the removed function anymore. **Verification (the sprint's own 5-step checklist, all live):** created a category with no group → `category_group_id: null`, no group materialized; created a real group, attached a category, deleted the group → category correctly became ungrouped, zero Default Group rows at any point; killed both backend node processes and ran a full fresh `npm run start:dev` → re-checked the DB → zero Default Group rows, confirming no startup-time recreation. `npx tsc --noEmit` exit 0 throughout. All temp verification scripts removed. |
| 2026-07-29 | Antigravity | SPRINT 28.8D Menu Builder Enterprise UX | Modified: `d4u-admin/src/pages/MenuManager.tsx`, `task.md`. | Converted `PRODUCTS` tab into a full-width, horiztonally-scrollable DataTable showing image, name, SKU, categories, groups, menus, recipe, station, price, status, and actions. Added multi-row delete and branch reassignment actions. Added top-level filters (Menu Collection, Category Group, Category, Status). Added CSV Import Preview Modal with robust Papaparse validation and explicit error highlighting before upload. Implemented HQ vs. Branch dynamic column logic to hide `Assigned Branches` when a specific store is selected. Wrapped "Menu Products Engine" forms into a collapsible accordion persisting state to `sessionStorage` to recover space above the table. Fixed type and syntax errors for final `tsc` build success. |
| 2026-07-29 | Claude | Sprint 28.8D: Menu Builder Enterprise Management (Backend) | New: `prisma/migrations/20260801040000_menu_builder_enterprise_management/`, `src/common/utils/{catalog-audit,sort}.util.ts`, `dto/bulk-assign-{category-group,product-category}.dto.ts`. Modified: `prisma/schema.prisma`, `catalog.service.ts`, `catalog.controller.ts`, `category-group.service.ts`/`.controller.ts`, `modifier.service.ts`/`.controller.ts`, `availability.service.ts`/`.controller.ts`. | **1. Bulk Category Group Assignment** — `POST /catalog/categories/bulk-assign-group` `{category_ids, category_group_id}`, one `$transaction`: validates the group and every category id exist (400 naming whichever are missing) before a single `updateMany` reassigns them all; `category_group_id: null` bulk-unassigns (groups are optional per the preceding Hotfix). **2. Bulk Product Category Assignment** — `POST /catalog/products/bulk-assign-category` `{product_ids, category_id}`, validates existence the same way, then a single parameterized `INSERT INTO "_ProductCategories" ... ON CONFLICT DO NOTHING` (one round trip, not N `connect` calls) — this *adds* the category to each product, it does not replace their existing ones. **3. Product List API** — `GET /catalog/products` now returns every field the spec lists (added `Product.updatedAt`, which didn't exist before — migration backfilled `CURRENT_TIMESTAMP`) plus flattened `category_id/_name`, `category_group_id/_name`, `menu_collection_id/_name`, `modifier_group_names[]`, `recipe_name`, `kitchen_station_name` (relation name, falls back to the legacy free-text column), `availability_rule_name` — computed in-memory from the same single query's nested includes, zero extra queries; the full `categories[]`/`modifierGroups[]`/`recipe`/`availabilityRule`/`kitchenStation` relation objects are still returned unchanged alongside them. **4. Category List API** — `GET /catalog/categories` now includes `product_count` via Prisma `_count` (one query, no per-category count query) alongside the existing `menu`/`categoryGroup`/`assigned_stores` relations. **5/6. CSV Export/Import rewritten** — export now emits the full spec'd column set (`product_name, sku, barcode, menu_collection, category_group, category, modifier_groups, recipe, kitchen_station, printer_group, kds_group, availability_rule, price, cost, margin, tax, status, description, image_url`); import now **strictly validates** every named Category, Category Group, Modifier Group, Availability Rule, Recipe, and Kitchen Station against existing store-scoped rows and errors the specific CSV row if any is missing — **no auto-creation of any master data**, a deliberate behavior change from the pre-28.8D importer (which silently created missing categories). Old export headers (`name`/`margin_pct`/`tax_rate`) still accepted on import via a header-alias map, so previously-exported CSVs still import unchanged. Printer Group / KDS Group remain free-text passthrough — no master-data table exists for either, so there's nothing to validate against (adding one would be a schema redesign, out of scope). **7. Sorting** — every listed list API (Categories, Products, Category Groups, Modifier Groups, Availability Rules, Menu Collections) now accepts `?sort_by=&sort_dir=asc\|desc`, validated against a per-endpoint allowlist (400 on an unrecognized field, via a new shared `resolveOrderBy` helper) instead of silently sorting wrong or throwing an opaque Prisma error. **8. Filters** — Products now accept `?category_id=&category_group_id=&menu_id=&status=&store_id=&search=` (search matches name/sku/barcode, case-insensitive); `store_id` stays optional (existing callers that always pass it are unaffected — it's simply enforced now instead of silently ignored) so an HQ view can list across every branch. **9. Performance** — added `Product(store_id, status)` and `Category(store_id, menu_id)` indexes (checked first for name collisions with Sprint 28.7's existing `Category_category_group_id_idx`); verified live that the full enriched Product List query executes as 8 SQL statements total regardless of row count (151 products), confirming no N+1. **10. Audit** — new `writeCatalogAudit` util (mirrors `writeKitchenAudit`'s generic shape) logs `BULK_CATEGORY_GROUP_ASSIGNED`, `BULK_PRODUCT_CATEGORY_ASSIGNED`, `PRODUCTS_CSV_IMPORTED`, `PRODUCTS_CSV_EXPORTED` to `SystemAuditLog`. Cross-checked (read-only) Antigravity's already-shipped `MenuManager.tsx` (row above) before finishing: fully compatible as-is — it reads `p.categories[].category_group_id`/`.menu_id` (raw scalars, unaffected by my additions), `p.modifierGroups`, `p.recipe`, `p.kitchen_station` exactly as before. Its bulk-assign handlers currently loop individual PATCH calls rather than using the new bulk endpoints — noted as a follow-up integration, not a defect. 27 live checks passed (both bulk endpoints' success + validation-error + transactional-rollback paths, enriched Product List fields/filters/sorting, Category List product_count, CSV export columns, CSV strict-validation rejecting a missing Category and a missing Category Group without creating either, sorting accepted on every listed endpoint, 4 audit action types confirmed written). `npx tsc --noEmit` exit 0. All test data and temp scripts cleaned up. |

---

## Session Checkpoint — 2026-07-29 (pre-compact, Claude)

**Current Sprint:** 28.5 — KDS Backend Integration Verification. Closed.

**Completed Tasks:** Sprint 28 (KDS Backend Foundation — full `KitchenModule`) and Sprint 28.5 (Integration Verification — live contract check, no backend changes needed) — see rows above for full detail.

**Files Modified:** See Sprint 28 and 28.5 rows above. Cumulative for this agent this session: 1 new Prisma migration, 1 new backend module (`src/modules/business/kitchen/`), 2 new shared utils, 1 new doc (`docs/API_CONTRACT.md`), plus modifications to `schema.prisma`, `seed-rbac.ts`, `app.module.ts`, `pos-orders.service.ts`, and both AI-team doc files. No frontend files touched.

**Pending Tasks:** None currently assigned.

**Known Issues:** Kitchen Stations settings UI and Inventory quick-adjust UI are still local-only, not wired to their corresponding (already-built, already-working) backend endpoints — frontend gap, not backend. Several `/kitchen/*` endpoints (chef-auth, dashboard, inventory-locks, recipe-availability) are built/verified but not yet called by any frontend code.

**Next Step:** Awaiting next sprint instruction (roadmap indicates Sprint 29 — Waiter Terminal, per Gemini's rows above).

---

## Re-verification — 2026-07-29 (Claude, Sprint 28.5 re-issued post-compaction)

| Date | Agent | Task | Files Changed | Summary |
|---|---|---|---|---|
| 2026-07-29 | Claude | Sprint 28.5 re-verification | None (verification only) | Re-ran the full live API-compatibility check after context compaction: `GET /kots?store_id=67`, `GET /inventory/items/67`, `GET /kitchen/stock-requests?store_id=67`, `GET /kitchen/tickets?store_id=67`, `GET /kitchen/dashboard?store_id=67` — all 200. `npx tsc --noEmit` exit 0. No drift since original Sprint 28.5 verification; no backend changes made. |
| 2026-07-29 | Antigravity | Sprint 28.8E | POS and Website frontend files | Complete Menu Hierarchy Integration: migrated frontends from legacy /catalog/sync to nested /catalog/category-groups/hierarchy endpoint. |
2026-07-29 | Antigravity | Sprint 5 CMS Modernization | d4u-admin/src/pages/CmsManager.tsx, MarketingHub.tsx, MenuManager.tsx | Replaced all remaining hardcoded tailwind colors with design tokens
2026-07-29 | Antigravity | Sprint 6B QA Fixes | d4u-website/src/App.tsx, LandingMode.tsx, MobileMode.tsx, StitchLanding.tsx, package.json | Fixed high-severity TypeScript build errors and configured oxlint for the website project.
2026-07-30 | Antigravity | Phase 2 Website CMS Modernization | d4u-admin/src/pages/CmsManager.tsx, MarketingHub.tsx, MenuManager.tsx, docs/API_CONTRACT.md | Reskinned Website CMS (CmsManager, MarketingHub, MenuManager) to Stitch Enterprise visual language; connected directly to live NestJS APIs; documented and implemented SEO CMS Meta tab with SERP preview and JSON-LD schema.
| 2026-09-23 | Antigravity | Sprint 29.3: Final Delivery Flow Fixes (Claimed Rider ID, Presence, Dispatch Guard, Pickup Flow, Website Scroll) | `d4u-pos-backend/src/app.gateway.ts`, `d4u-pos-backend/src/modules/business/rider/rider.controller.ts`, `d4u-pos-backend/src/modules/business/rider/rider.service.ts`, `d4u-pos-backend/src/modules/business/rider/rider.service.spec.ts`, `d4u-pos-client/src/App.tsx`, `d4u-rider/src/App.tsx`, `d4u-rider/src/components/ActiveRideView.tsx`, `d4u-website/src/index.css`, `d4u-website/src/components/Hero3DCanvas.tsx`, `d4u-website/src/components/OrderTrackerModal.tsx`, `d4u-website/src/components/shared/CartDrawer.tsx`, `docs/AI_TEAM_PROGRESS.md` | Fixed POS claimed rider hydration & removed all fake Active Rider fallbacks; added POS Dispatch guard requiring PRINT_BILL & real claimed rider; added real-time in-memory rider presence tracking in gateway with availability endpoint & client broadcasts; enforced backend single-active-delivery claim protection with 409 Conflict alongside atomic claim; rider App arrival triggers RIDER_ARRIVED only if order is READY; Rider App Confirm Picked Up strictly guarded behind DISPATCHED status; resolved Website scroll blocking by adding overflow styling to body, pointer-events-none to 3D canvas, and body overflow lock/unlock on drawer/modal open/close. |
| 2026-09-24 | Agent 07 (Antigravity) | Remediation Batch 1: Dexie Sync & Scope Hardening | `d4u-pos-client/src/db.ts`, `d4u-pos-client/src/StitchKDS.tsx`, `d4u-pos-client/src/pages/TvBoard.tsx`, `d4u-pos-client/src/utils/staleSyncProtection.test.ts`, `d4u-pos-client/test-multitab-browser.cjs` | Remediated 5 Codex audit findings: (1) Mandatory businessDayId in sync path with safe abort on invalid identity; (2) Removed bd_all fallback from getSyncScopeKey; (3) Cross-tab atomic sequence issuance using Web Locks API (navigator.locks.request) and in-process mutex queue; (4) Added real browser multi-tab test with Puppeteer verifying concurrent distinct sequences and stale snapshot rejection; (5) Scoped duplicate reconciliation strictly to store_id + businessDayId + backendKotId. |
| 2026-09-24 | Agent 07 (Antigravity) | Remediation Batch 2: Codex Findings Remediation | `d4u-pos-client/src/db.ts`, `d4u-pos-client/src/StitchKDS.tsx`, `d4u-pos-client/src/pages/TvBoard.tsx`, `d4u-pos-client/src/TVDisplay.tsx`, `d4u-pos-client/test-harness.html`, `d4u-pos-client/test-multitab-browser.cjs`, `d4u-pos-client/src/utils/staleSyncProtection.test.ts`, `d4u-pos-client/src/utils/kotSyncIdentity.test.ts` | Remediated remaining Codex findings: (1) Browser multi-tab test loads and executes literal production db.ts via Vite test harness; (2) Unsupported Web Locks browsers fail closed safely without unsafe cross-tab fallback; (3) Multi-process atomic sequence coordination backed by IndexedDB under Web Locks; (4) KDS, TvBoard, and TVDisplay authoritatively verify business day from backend and abort sync without falling back to cached ID; (5) Completely removed unscoped backendKotId map alias; (6) Inner stale transaction detection communicates applied: false with STALE_SNAPSHOT. |
| 2026-09-24 | Agent 07 (Antigravity) | Remediation Batch 4: IndexedDB Storage Fail-Closed & KDS/TV Authoritative Business-Day Verification | `db.ts`, `StitchKDS.tsx`, `TvBoard.tsx`, `TVDisplay.tsx`, `test-harness.html`, `test-multitab-browser.cjs`, `staleSyncProtection.test.ts`, `kdsTvBusinessDayVerification.test.ts` | Fixed Issue 1: Mandatory IndexedDB in browser environments. If IndexedDB is unavailable, opening fails, reading fails, or writing fails, acquireSyncSequence and getLatestAppliedSyncSequence throw SYNC_STORAGE_UNAVAILABLE, zero sequences allocated, zero in-memory fallback, and syncAndReconcileBackendKots safely returns applied: false with SYNC_STORAGE_UNAVAILABLE without Dexie transaction, bulkDelete, or bulkPut. Verified across multi-tab browser contexts that two tabs cannot receive duplicate sequences. Fixed Issue 2: KDS, TV Board, and TVDisplay initialize activeBusinessDayId as null for rendering. Cached per-store day cannot authorize rendering before backend verification. If /business-day/current fails, times out, 404s, or returns malformed/invalid id, activeBusinessDayId is set to null, cache is removed, reconciliation is skipped, and zero KOTs render. Authoritative valid positive integer day renders matching KOTs and hides wrong day. All 132 vitest tests passed, 7 real browser multi-tab tests passed, tsc clean, vite build clean. |



